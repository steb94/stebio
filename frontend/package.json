{
  "scripts": {
    "build": "rm -rf build && mkdir -p build && cp -r index.html product.html store.html dashboard.html support.html app.js product.js store.js dashboard.js support.js style.css 404.html robots.txt build/"
  }
}
